apiVersion: batch/v1
kind: Job
metadata:
  name: migration-job-1df196ea21e
  namespace: candoo-test
spec:
  template:
    spec:
      containers:
        - name: candoo-migration
          image: docker.repo.taavsystem.ir/service-charge-argo-migrations:1df196ea21e
          envFrom:
            - secretRef:
                name: candoo-back-secret
      restartPolicy: OnFailure
      imagePullSecrets:
        - name: private-repo